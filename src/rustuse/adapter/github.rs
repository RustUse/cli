//! GitHub workflow inspection for RustUse facade repositories.
//!
//! Only shared GitHub Actions workflow presence checks are wired into the
//! current report commands. Richer `.github` policy/check/report logic is not
//! part of this release.

pub(crate) mod workflows;
